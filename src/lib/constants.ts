/** Constantes compartidas entre cliente y servidor. */

/** Maximo de caracteres del campo "Location, Country". */
export const LOCATION_MAX_LENGTH = 80;

/** Maximo de caracteres del campo "Taken on". */
export const DEVICE_MAX_LENGTH = 60;

/** Fotos que se pueden subir de una sola tacada. */
export const MAX_FILES_PER_UPLOAD = 12;
